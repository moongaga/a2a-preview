import React from 'react';
import { ArrowRight } from 'lucide-react';
import type { RouteState } from '../model/router';
export function ValueStreamCards({ navigate, role }: { navigate: (route: Partial<RouteState>, replace?: boolean) => void; role: string }) { const streams = [{ id: 'leads', name: '线索转化', metric: role === 'business' ? '29.8%' : '—', status: '诊断运行中' }, { id: 'content', name: '内容生产', metric: role === 'trainer' ? '92%' : '72%', status: '品牌审核中' }, { id: 'commerce', name: '商城运营', metric: role === 'employee' ? '只读' : '¥286万', status: '权益审批中' }, { id: 'koc', name: 'KOC运营', metric: '1.84', status: '履约验收中' }]; return <section className="content-panel"><div className="panel-heading"><h2>业务价值流</h2></div>{streams.map((stream) => <button className="journey-row" type="button" key={stream.id} onClick={() => navigate({ page: 'scenario', scenario: stream.id }, true)}><div><strong>{stream.name}</strong><small>{stream.status}</small></div><b>{stream.metric}</b><ArrowRight size={14} /></button>)}</section>; }
